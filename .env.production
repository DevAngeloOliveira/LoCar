# Variáveis de ambiente para produção (Render)
# Estas variáveis devem ser configuradas no painel do Render

# DATABASE_URL é fornecido automaticamente pelo Render ao conectar o PostgreSQL
# DATABASE_URL="postgresql://user:password@host:5432/database"

NODE_ENV=production
PORT=3000

# CORS - Adicionar domínio da Vercel quando disponível
# FRONTEND_URL=https://seu-app.vercel.app
