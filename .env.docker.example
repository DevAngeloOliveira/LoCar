# Variáveis de ambiente para uso com Docker Compose
# Copie este arquivo para .env quando subir os containers

NODE_ENV=production
PORT=3000
DATABASE_URL="postgresql://locar:locar123@postgres:5432/locar?schema=public"
FRONTEND_URL="http://localhost:3001"
