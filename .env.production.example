# Variáveis de ambiente para produção
# Configure estas variáveis no painel do provedor (Render, Railway, etc.)

NODE_ENV=production
PORT=3000

# Fornecido automaticamente pelo banco gerenciado:
# DATABASE_URL="postgresql://user:password@host:5432/database?schema=public"

# Em produção FRONTEND_URL é obrigatório para CORS restrito:
# FRONTEND_URL="https://seu-frontend.exemplo.com"
